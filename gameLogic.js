var gameLogic = function(thisPlayer) {
  var app = window.raysistanceApp;
  if (window.playerName === thisPlayer.get('name')) {
    alert('your are the leader!!');
    that.distributeIdentities();
  }
};